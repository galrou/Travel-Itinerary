# Graph node names, shared between nodes.py (route return values) and
# graph.py (node registration/edges) so the two can't drift out of sync.
NODE_REQUIREMENTS = "requirements"
NODE_RESEARCH = "research"
NODE_SEQUENCE = "sequence"
NODE_RESTAURANT_RESEARCH = "restaurant_research"
NODE_RESTAURANT_SELECTION = "restaurant_selection"
NODE_FINALIZE = "finalize"
ROUTE_END = "end"
